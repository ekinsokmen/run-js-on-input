// Sample script to add line number to input 
//
// context is an empty object provided to the script. 
(context.i)? context.i++: context.i=1;

// Print out line number and line
console.log(context.i, input);
